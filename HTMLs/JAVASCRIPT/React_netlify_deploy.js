/**
 *  Create a dist folder using the command in the directory using terminal "npm run build"
 *  Then go to netlify and deploy that dist folder. done
 * and for surge, write surge dist and go on
 * Public folder will have a file named CNAME
 * and we will add the domain name there for surge
 * 
 * 
 */